<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Transparent background (no fill) -->
  <!-- Outer circle ring -->
  <circle cx="50" cy="50" r="45" fill="none" stroke="#1C4B42" stroke-width="3"/>
  <!-- Inner circle -->
  <circle cx="50" cy="50" r="35" fill="#1C4B42"/>
  <!-- Accent dot -->
  <circle cx="50" cy="35" r="6" fill="#9EDC2A"/>
</svg>